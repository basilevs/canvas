---
goal: Implement video-like history replay (with inactivity compression) and single-step undo for the collaborative whiteboard
version: 1.0
date_created: 2026-06-13
last_updated: 2026-06-25
owner: basilevs
status: 'Completed'
tags: [feature, replay, history, undo, animation, frontend, rest-api]
---

# Introduction

![Status: Completed](https://img.shields.io/badge/status-Completed-brightgreen)

Implement a video-like replay system that allows users to watch the board's drawing history animated chronologically. The replay skips periods of inactivity (configurable threshold), animates individual strokes point-by-point using stored temporal metadata, and provides VCR-style playback controls.

Because replay is the sole consumer of the append-only event log, this plan also **introduces** that foundation: the `StrokeEvent` document, `StrokeEventService`, the `StrokeEvents` **native time-series collection**, and the layering of event-append onto the core hub `SendStroke`. The append-only event log becomes the **single source of truth** for board state. Strokes are delivered to clients via `StrokeReceived` events (not from a snapshot). Volatile metadata (user list, etc.) is updated via dedicated metadata events. Board snapshots are eliminated as a concept (TASK-007 retires the snapshot write path, the `/snapshot` REST endpoint, and the `Board.ActiveStrokes` field); in Phase 0, clients build board state purely from the event stream. (Later, [feature-history-compaction-1.md](./feature-history-compaction-1.md) will introduce keyframes as an optimization.) This plan builds upon [feature-collaborative-whiteboard-1.md](./feature-collaborative-whiteboard-1.md) (the MVP) — the MVP is finalized and will not be modified.

This plan also **owns undo** (single-step removal of the caller's last stroke). Undo is the producer of `Remove` `StrokeEvent`s, so it belongs with the event log it depends on: it queries the log for the caller's most recent removable stroke (a bounded, index-backed lookup — never a full-history scan), records a `Remove` event, and broadcasts the removal. Undo (Phase 5) depends only on the Phase 0 event-log foundation and can be implemented immediately after it, independently of the replay UI (Phases 1–4).

## 1. Requirements & Constraints

- **REQ-001**: Users can trigger a replay mode that animates the board's stroke history in chronological order
- **REQ-002**: Replay uses `Stroke.Timestamp` for inter-stroke timing and `Point.TimeOffset` for intra-stroke point-by-point animation
- **REQ-003**: Inactivity gaps exceeding a configurable threshold (default 3 seconds) are compressed/skipped during playback
- **REQ-004**: During replay, the progress display shows the actual wall-clock `Stroke.Timestamp` of the currently animated stroke, while the scrubber position and total replay duration use compressed time
- **REQ-005**: Playback speed is adjustable: 1x, 2x, 4x multipliers
- **REQ-006**: VCR controls: play, pause, seek (timeline scrubber), stop (exit replay)
- **REQ-007**: Within this plan the history endpoint is **open/unauthenticated** (no membership gate, no caller identity) and serves the **complete, unfiltered** stroke history. The **membership access gate** (reject non-members with `403`) and owner-controlled visibility filtering of replay (HiddenRanges cut-offs) are separate concerns layered on by [feature-history-access-moderation-1.md](./feature-history-access-moderation-1.md), which depends on the membership and HiddenRange models from the administration/moderation plans
- **REQ-008**: Replay data is served by a dedicated REST endpoint using fixed path-based pagination (no query parameters), ordered **oldest-first (chronological)** so that pages `1..N-1` are immutable and only the final (newest) page grows as strokes are added. Requesting a non-existent page returns `404`; a board that exists but has **no events** returns `404` for page `1`, which clients treat as "no history"
- **REQ-009**: History pages are cached so that during normal navigation **complete (immutable) pages are never re-requested from the server**, while a manual reload still revalidates cheaply (yielding `304 Not Modified`); the final, still-growing page is always revalidated. Accepted limitation: an event appended to the final page **within the same wall-clock second** as the cached representation may not surface via a conditional REST GET until the next second — harmless because the live SignalR channel delivers that event incrementally regardless
- **REQ-010**: Joining a board **atomically** subscribes the connection to live updates and replays the missed event tail through the **same** client channel used for live broadcasts, so that across the REST-history / live-sync boundary **no event can be missed or duplicated** at join time. History fetching (cacheable REST) stays separate from live sync (SignalR), and the join is snapshot-free
- **REQ-011**: When joining, a client first loads the complete history from the cacheable REST endpoint, then catches up only the newer tail via the single join call and continues receiving live events — rendering full history, tail, and live updates **de-duplicated by stroke `Id`** with no gaps
- **REQ-012**: This plan introduces the append-only event log: every accepted stroke is recorded as an `Add` `StrokeEvent`, and events are ordered chronologically by `Timestamp` (ties broken by the stable stroke `Id`) with no per-board `SequenceNumber`
- **REQ-013**: A user can undo their own most recently drawn stroke; undo records a `Remove` `StrokeEvent` (the authoritative log entry) and broadcasts the removal so every connected client updates. Undo only affects the caller's own strokes and is a no-op when the caller has no remaining strokes
- **CON-001**: Frontend is vanilla JavaScript on HTML5 Canvas (no framework)
- **CON-002**: Backend is ASP.NET Core 10.0 with MongoDB Atlas
- **CON-003**: `StrokeEvent` documents carry `Timestamp` (the `timeField`), `Type`, and `Stroke.Points[].TimeOffset`, and are persisted in a **native time-series collection** (`metaField` = `BoardId`, `granularity` = `seconds`). The `Stroke`/`Point` temporal fields are reused from the MVP data model, but the event-log document and its collection are introduced by this plan (Phase 0)
- **GUD-001**: Deriving the current board picture by folding the log (Add/Remove → visible strokes, dedup by stroke `Id`) is the **client's** concern — it already downloads the paginated history. The **server never materializes the full log in memory**: server operations are bounded — paginated reads (endpoint), since-timestamp reads (incremental sync), and a single-document index-backed lookup for undo's last removable stroke

## 2. Implementation Steps

### Implementation Phase 0

- GOAL-000: Introduce the append-only event log foundation (relocated from the MVP) that records every stroke for later replay

| Task | Description | Completed | Date |
|------|-------------|-----------|------|
| TASK-001 | Create `Models/StrokeEvent.cs` — append-only event document: `Id` (`ObjectId`), `BoardId` (string — the time-series `metaField`), `Type` (enum `EventType { Add, Remove }`), `Stroke` (embedded `Stroke`, reusing the MVP model with its `UserId`, `Id`, and `Points[].TimeOffset` temporal metadata), `Timestamp` (UTC `DateTime` — the time-series `timeField`, native BSON date). Stored in the `StrokeEvents` native time-series collection (TASK-004). No `SequenceNumber` — ordering is by `Timestamp`, with the embedded stroke's stable `Id` as the tiebreaker. `Add` events are produced by `SendStroke` (Phase 0); `Remove` events are produced by undo (Phase 5). | ✅ | 2026-06-17 |
| TASK-002 | Add a `StrokeEvents` typed collection accessor to `Services/MongoDbContext.cs` (and `IMongoDbContext`), extending the MVP context which exposes only `Boards` and `Users`. | ✅ | 2026-06-17 |
| TASK-003 | Create `Services/StrokeEventService.cs` (and `IStrokeEventService`) as the persistence/query layer with: `AppendEventAsync(boardId, EventType type, Stroke stroke, CancellationToken)` (stamps the server `Timestamp` and inserts the measurement; **the insert-time de-duplication authority** — since the time-series collection supports no unique index (TASK-004), it de-duplicates `Add` events at the application level by the stroke's client-generated `Id`, skipping the insert when an `Add` for that `Id` already exists on the board, so a stroke re-sent on reconnect is never double-logged; this is the role the MVP located in the snapshot `$push`, now moved to the authoritative log), `GetEventsPageAsync(boardId, pageNumber, pageSize, CancellationToken)` (oldest-first page slice plus total counts, for the REST endpoint), `GetEventsSinceAsync(boardId, sinceTimestamp, CancellationToken)` (inclusive `Timestamp >= sinceTimestamp` tail read for the join replay — inclusive so no boundary event is dropped; the client de-duplicates by stroke `Id`), and `GetLastRemovableStrokeByUserAsync(boardId, userId, CancellationToken)` (**bounded, index-backed** lookup for undo — walks the caller's own `Add` events **newest-first** (backed by the `(Stroke.UserId, Timestamp)` index, paging a small bounded window rather than the whole log) and returns the first `Add` whose stroke `Id` has **no** later matching `Remove`, or `null` when every recent `Add` has been removed; not a literal `limit(1)`, since the newest `Add` may already be removed). Every method takes a `CancellationToken` (forwarded to the MongoDB driver and on to the endpoint/hub callers, consistent with the existing `IBoardService` signatures). Register `IStrokeEventService` in DI in `Program.cs`. | ✅ | 2026-06-17 |
| TASK-004 | Create the `StrokeEvents` collection as a **native MongoDB time-series collection**: `db.createCollection("StrokeEvents", { timeseries: { timeField: "Timestamp", metaField: "BoardId", granularity: "seconds" } })`. This auto-provisions the internal clustered `{ metaField, timeField }` index that serves per-board chronological reads (paginated endpoint + incremental cursor). Add a secondary index on the embedded stroke `Id` (to resolve a `Remove`'s target and for read-side de-duplication) and a secondary index on `(Stroke.UserId, Timestamp)` to back the bounded last-removable-stroke lookup for undo. Note: time-series collections **do not support unique indexes**, so per-board ordering relies on the `timeField` rather than a DB-enforced unique sequence. Because a native time-series collection **cannot be created lazily** by `GetCollection<>` + first insert (that would yield an ordinary collection), add an **idempotent `EnsureIndexesAsync`** to `StrokeEventService`/`MongoDbContext` that creates the collection via the driver's `CreateCollectionAsync` with `TimeSeriesOptions` when it is absent, then creates the two secondary indexes; call it at startup in `Program.cs` alongside the existing `BoardService`/`UserProfileService` `EnsureIndexesAsync` calls. The `db.createCollection(...)` shell snippet above is illustration only; integration tests (TASK-006, TASK-015) rely on this programmatic creation against the test database. | ✅ | 2026-06-17 |
| TASK-005 | Layer the append-only event log onto the core hub `SendStroke` (MVP TASK-026): call `StrokeEventService.AppendEventAsync(boardId, EventType.Add, stroke)` (the durable record and the idempotency/de-duplication authority — TASK-003), then broadcast `StrokeReceived(stroke)` via the `StrokeReceived` client method (see GUD-009, use the typed proxy, never `SendAsync`). The event log is the single source of truth; strokes are delivered to clients via events (not from a snapshot). Log append comes first so history is durable before clients receive the stroke broadcast. This is the only producer of `Add` events in scope. **Remove the MVP snapshot write**: delete the existing `_boardService.AddStrokeToSnapshotAsync(...)` call (the snapshot `$push`) from `SendStroke` — its de-duplication/idempotency role moves to `AppendEventAsync` (TASK-003), and the remaining snapshot surface is retired in TASK-007. | ✅ | 2026-06-17 |
| TASK-006 | Add MSTest integration tests for the event log in `Tests/StrokeEventServiceTests.cs` (relocated from the MVP): verify `AppendEventAsync` stamps a server `Timestamp`, that `GetEventsPageAsync` returns events oldest-first with correct total counts, that a re-sent `Add` with a duplicate stroke `Id` is not double-logged, and that `GetLastRemovableStrokeByUserAsync` returns the caller's most recent non-removed `Add` (and `null` when none remain). Exercises a MongoDB test database. | ✅ | 2026-06-17 |
| TASK-007 | Retire the now-eliminated board-snapshot surface (the event log is the single source of truth): remove the `GET /api/boards/{name}/snapshot` endpoint and the `MapBoardSnapshot` helper from `Program.cs`; delete `Dtos/BoardSnapshotResponse.cs`; remove `LoadSnapshot` from `Hubs/IWhiteboardClient.cs`; remove `GetSnapshotAsync` and `AddStrokeToSnapshotAsync` from `Services/BoardService.cs` (+ `IBoardService`); and remove the `ActiveStrokes` field from `Models/Board.cs` along with its `SetOnInsert(..., ActiveStrokes, [])` initialization in `GetOrCreateBoardAsync`. Update any MVP tests that referenced the snapshot endpoint or `LoadSnapshot`. (TASK-005 already removed the snapshot write from `SendStroke`; TASK-012 removes the snapshot read path from `JoinBoard`.) | ✅ | 2026-06-17 |

### Implementation Phase 1

- GOAL-001: Implement the REST API endpoint that serves replay history with path-based pagination and HTTP caching

| Task | Description | Completed | Date |
|------|-------------|-----------|------|
| TASK-008 | Add REST endpoint `GET /api/boards/{name}/history/{pageNumber}` in `Program.cs` with fixed path-based pagination (no query parameters). Returns paginated history in **chronological (oldest-first) order** with a large fixed page size optimized for throughput (default 5000 events per page). Route: `app.MapGet("/api/boards/{name}/history/{pageNumber}", ...)`. Follow the same OpenAPI metadata pattern the MVP snapshot endpoint established before its removal in TASK-007 (the built-in `AddOpenApi`/`MapOpenApi` pipeline): chain `.WithName("GetBoardHistory").WithSummary("Gets a page of board stroke history").Produces<HistoryPageResponse>(StatusCodes.Status200OK).Produces(StatusCodes.Status304NotModified).Produces(StatusCodes.Status400BadRequest).Produces(StatusCodes.Status404NotFound)`. | ✅ | 2026-06-17 |
| TASK-009 | Implement the endpoint handler with an explicit typed return signature `async Task<Results<Ok<HistoryPageResponse>, BadRequest, NotFound>>` using `TypedResults` (consistent with the MVP snapshot endpoint's `TypedResults` pattern, retired in TASK-007), plus the custom `304` branch via `TypedResults.StatusCode(StatusCodes.Status304NotModified)` (there is no `TypedResults` 304 helper). Normalize the URL slug `{name}` to its canonical `boardId` via `BoardNameNormalizer.TryNormalizeBoardName`; **return `400 BadRequest` when the slug fails to normalize** and **when `pageNumber < 1`** (mirroring the `BadRequest` path the MVP snapshot endpoint used). Fetch the board, returning `404` if absent; return `404` if the page exceeds total pages (a board with no events returns `404` for page `1`, per REQ-008). Forward the endpoint's `CancellationToken` to every downstream service call. On its own this plan applies **no** membership gate and requires **no** caller identity — the endpoint is **open/unauthenticated within this plan** (REQ-007); the `IsMemberAsync` `403` gate and caller identity resolution are layered on later by [feature-history-access-moderation-1.md](./feature-history-access-moderation-1.md). | ✅ | 2026-06-17 |
| TASK-010 | Return the page of `StrokeEvents` for the board in **chronological (oldest-first) order** via `GetEventsPageAsync` (TASK-003), projected into response DTOs per GUD-003 (never serialize the domain `StrokeEvent`/`Stroke`/`Point` directly). Add `Dtos/HistoryPageResponse.cs` — `sealed record HistoryPageResponse(int PageNumber, int TotalEvents, int TotalPages, IReadOnlyList<StrokeEventResponse> Events)` — and `Dtos/StrokeEventResponse.cs` — `sealed record StrokeEventResponse(string Id, string Type, StrokeResponse Stroke, DateTime Timestamp)` (UTC `DateTime`, never `DateTimeOffset`), each carrying a `<summary>` XML doc comment (flows into the OpenAPI schema, matching the existing DTOs), reusing the existing `StrokeResponse`/`PointResponse` DTOs (which carry `id`, `userId`, `color`, `width`, `points[]` with `timeOffset`, `timestamp` — note `StrokeResponse` has no `duration`). Map the events to these DTOs in the endpoint handler. The serialized JSON is `{ "events": [ { "id", "type", "stroke": { ... }, "timestamp" } ], "pageNumber": 1, "totalEvents": N, "totalPages": M }`. | ✅ | 2026-06-17 |
| TASK-011 | Set per-page cache headers based on completeness (REQ-009), always including a `Last-Modified` header equal to the timestamp of the page's most recent event **truncated to whole seconds** (HTTP-date precision). For a **complete page** (`pageNumber < totalPages`, full and immutable): emit `Cache-Control: public, max-age=31536000` (no `immutable`, so a manual reload still revalidates). For the **final (growing) page** (`pageNumber == totalPages`): emit `Cache-Control: no-cache`, forcing revalidation on every use. Distinguishing the two requires computing `totalPages` (already available from TASK-010's counts). | ✅ | 2026-06-17 |
| TASK-012 | Implement conditional GET handling via the `If-Modified-Since` request header for **every** page (complete pages won't poll during their freshness lifetime, but a manual reload revalidates them; the final page revalidates on every use): compare the request header timestamp with the page's `Last-Modified` timestamp (the most recent event on that page) **at one-second granularity** — truncate the page's most-recent-event timestamp to whole seconds before comparing, otherwise the millisecond `Timestamp` never matches the whole-second `If-Modified-Since` and the handler always returns `200`. If `If-Modified-Since >=` the truncated page timestamp, return `304 Not Modified` (empty body, cached representation reused). Otherwise return `200` with full page content. A complete page always yields `304` on revalidation since its content can never change; the final page yields `304` while no newer-second event exists on it. **Accepted limitation** (REQ-009): an event appended within the same wall-clock second as the cached representation is not surfaced by a conditional GET until the next second; the live SignalR channel delivers it incrementally in the meantime. | ✅ | 2026-06-17 |
| TASK-013 | Extend the core hub `JoinBoard` (MVP) into the **atomic** subscribe-and-catch-up method `JoinBoard(boardName, sinceTimestamp)` in `Hubs/WhiteboardHub.cs`: normalize the slug to `boardId`, **first** add the connection to the board group (subscribe to live broadcasts), **then** replay the tail — every `StrokeEvent` (both `Add` and `Remove`) with `Timestamp >= sinceTimestamp` via `GetEventsSinceAsync`, ordered chronologically — to the **calling connection only** through the **same** typed `StrokeReceived`/`StrokeRemoved` client methods used for live broadcasts (never `SendAsync`). Subscribing before replaying closes the join race; the inclusive `>=` bound guarantees no boundary event is dropped, and the client de-duplicates by stroke `Id`. **Remove the MVP snapshot path** (`GetSnapshotAsync` / `LoadSnapshot`) from `JoinBoard` — this plan derives client state from the event stream, not a snapshot. Forward a `CancellationToken` (e.g. `Context.ConnectionAborted`) to `GetEventsSinceAsync`. | ✅ | 2026-06-17 |
| TASK-014 | Implement join-time history population in `wwwroot/js/app.js` (REQ-011) using a shared `fetchAllHistory(boardName)` helper (TASK-018) that fetches all pages from `GET /api/boards/{boardName}/history/1`, `/history/2`, … oldest-first (auto-paginating until a `404`, each page cached independently; a `404` on page `1` means an empty board → no history). On join, call `fetchAllHistory`, extract the `Timestamp` of the last stroke on the final page (or the Unix epoch when there is no history), connect to the Hub, and call `JoinBoard(boardName, sinceTimestamp)` once — which atomically subscribes and replays the tail through the live `StrokeReceived`/`StrokeRemoved` handlers. Render full history and the replayed tail to the canvas, de-duplicating by stroke `Id`, then keep receiving live events on the same channel. | ✅ | 2026-06-17 |
| TASK-015 | Add integration test `Tests/ReplayHistoryEndpointTests.cs`: verify pagination metadata (`totalEvents`, `totalPages`), oldest-first ordering, that a **complete page** carries `Cache-Control: public, max-age=31536000` **without** `immutable` and a `Last-Modified` equal to its most recent stroke, that the **final page** carries `Cache-Control: no-cache` with a `Last-Modified` equal to its most recent stroke, that a conditional GET with `If-Modified-Since` returns `304` for both an unchanged final page and a complete page (manual-reload revalidation), that requesting a non-existent page returns `404`, and that all pages combined contain the complete unfiltered history. (The membership-`403` gate and HiddenRanges filtering tests live in [feature-history-access-moderation-1.md](./feature-history-access-moderation-1.md).) | ✅ | 2026-06-17 |
| TASK-016 | Extend `canvas.http` (the repo's existing request file) with the new history endpoint so it is exercisable by hand: a request for `GET /api/boards/{name}/history/1`, a request for a non-existent page (expect `404`), and a conditional request carrying `If-Modified-Since` (expect `304`). Also **remove the now-deleted `/snapshot` request** from `canvas.http` (the endpoint is retired in TASK-007). Match the base URL/port already used in `canvas.http`. | ✅ | 2026-06-17 |

### Implementation Phase 2

- GOAL-002: Implement the frontend replay engine with temporal animation and gap compression

| Task | Description | Completed | Date |
|------|-------------|-----------|------|
| TASK-017 | Create `wwwroot/js/replay.js` — export `ReplayEngine` class with constructor accepting a canvas 2D context reference and configuration `{ gapThresholdMs: 3000, speed: 1 }`. | ✅ | 2026-06-17 |
| TASK-018 | Implement `ReplayEngine.loadHistory(boardName)` — fetch all pages via the shared `fetchAllHistory(boardName)` helper (defined once and reused by the join flow, TASK-014): it auto-paginates `GET /api/boards/{boardName}/history/1`, `/history/2`, … oldest-first until a `404` is encountered (a `404` on page `1` means no history → empty result). Store the returned events in `this.events` sorted by `timestamp` (ties broken by stroke `id`). | ✅ | 2026-06-17 |
| TASK-019 | Implement `ReplayEngine.computeTimeline()` — precompute a timeline array: for each event, calculate `realTimeGapMs` (time since previous event's timestamp). If `realTimeGapMs > gapThresholdMs`, compress to `gapThresholdMs`. Store `playbackOffsetMs` (cumulative adjusted time) for each event. Compute `totalDurationMs` for the entire replay. | ✅ | 2026-06-17 |
| TASK-020 | Implement `ReplayEngine.play()` — start a `requestAnimationFrame` loop. Track `elapsedMs` using `performance.now()` scaled by `this.speed`. For each frame, determine which events should have started based on `elapsedMs` vs `playbackOffsetMs`. For active strokes, animate points using `Point.TimeOffset` scaled by speed. | ✅ | 2026-06-17 |
| TASK-021 | Implement intra-stroke animation: when rendering a stroke in progress, draw points incrementally — only points where `(elapsedMs - strokeStartMs) * speed >= point.TimeOffset` are rendered. Use `ctx.beginPath()` / `ctx.lineTo()` for smooth progressive drawing. | ✅ | 2026-06-17 |
| TASK-022 | Implement `ReplayEngine.pause()` — pause the `requestAnimationFrame` loop, store current `elapsedMs`. | ✅ | 2026-06-17 |
| TASK-023 | Implement `ReplayEngine.resume()` — resume from stored `elapsedMs`. | ✅ | 2026-06-17 |
| TASK-024 | Implement `ReplayEngine.seek(positionRatio)` — accept 0.0–1.0, compute target `elapsedMs = positionRatio * totalDurationMs`, clear canvas, re-render all events that complete before target time (instant render), then set playback position for animation to continue from there. | ✅ | 2026-06-17 |
| TASK-025 | Implement `ReplayEngine.setSpeed(multiplier)` — accept 1, 2, or 4. Adjust elapsed time tracking so playback continues smoothly from current position at new speed. | ✅ | 2026-06-17 |
| TASK-026 | Implement `ReplayEngine.stop()` — stop animation loop, clear canvas, fire `onStop` callback to notify UI. | ✅ | 2026-06-17 |
| TASK-027 | Handle `Remove` events in replay: when a Remove event's playback time is reached, erase the referenced stroke from the canvas (re-render all prior visible strokes excluding removed ones). | ✅ | 2026-06-17 |

### Implementation Phase 3

- GOAL-003: Implement the replay UI controls and integrate with the whiteboard page

| Task | Description | Completed | Date |
|------|-------------|-----------|------|
| TASK-028 | Add replay controls to `wwwroot/index.html`: (1) In toolbar: add replay button (`<button id="btn-replay">▶ Replay</button>` — **always visible**), plus replay-mode-only controls (play/pause button, stop button, speed selector, timestamp display — **hidden by default, shown during replay**). (2) Under canvas: add `<div id="replay-controls">` containing the timeline scrubber (`<input type="range" id="replay-scrubber" min="0" max="1" step="0.001">` — **hidden by default, shown during replay**). | ✅ | 2026-06-17 |
| TASK-029 | Wire replay button click (in toolbar): instantiate `ReplayEngine(canvasCtx, config)`, call `loadHistory(boardName)`, show the replay-controls div and replay-mode toolbar buttons (set `display: block`/`inline-block`), disable drawing tools, call `play()`. | ✅ | 2026-06-17 |
| TASK-030 | Wire replay-mode controls during replay: play/pause button toggles `pause()`/`resume()`, stop calls `stop()` and hides the replay-controls div and replay-mode toolbar buttons + re-enables drawing tools + resyncs board state by refetching all events from the history endpoint, scrubber `input` event calls `seek(value)`, speed selector `change` calls `setSpeed(value)`. | ✅ | 2026-06-17 |
| TASK-031 | Wire `ReplayEngine.onProgress` callback: update scrubber position based on compressed playback time (`currentCompressedMs / totalCompressedDurationMs`), and display the wall-clock `Timestamp` of the currently animated stroke formatted as ISO 8601 or localized date/time string. | ✅ | 2026-06-17 |
| TASK-032 | Wire `ReplayEngine.onStop` callback: hide the replay-controls div and replay-mode toolbar buttons (set `display: none`), restore drawing tools, resync board state by refetching all events from the history endpoint to rebuild the current canvas. | ✅ | 2026-06-17 |
| TASK-033 | Add CSS in `wwwroot/css/style.css` for the replay controls: the replay-controls div positioned directly under the canvas with full width, styled scrubber input. The replay-controls div and replay-mode toolbar controls hidden by default (`display: none`); the replay button always visible. | ✅ | 2026-06-17 |

### Implementation Phase 4

- GOAL-004: Testing and validation of replay functionality

| Task | Description | Completed | Date |
|------|-------------|-----------|------|
| TASK-034 | Unit test `Tests/ReplayEngineTests.js` (or inline in an HTML test page): verify `computeTimeline()` compresses gaps > 3s, verify `totalDurationMs` is correct, verify `seek` renders the correct state at the midpoint. | ✅ | 2026-06-17 |
| TASK-035 | Manual test — Draw 5 strokes with 10+ second pauses between some. Click replay. Verify inactivity gaps are compressed, strokes animate point-by-point, total replay duration is much shorter than real elapsed time. Verify the textual timestamp display shows the actual wall-clock time of each stroke (not compressed). | ✅ | 2026-06-25 |
| TASK-036 | Manual test — During replay, click pause, move scrubber to 50%, resume. Verify canvas shows correct state at midpoint and continues animating from there. | ✅ | 2026-06-25 |
| TASK-037 | Manual test — Change speed from 1x to 4x during playback. Verify animation speeds up smoothly without jumping. | ✅ | 2026-06-25 |

### Implementation Phase 5

- GOAL-005: Implement single-step undo (remove the caller's last stroke) on top of the Phase 0 event log. Depends only on Phase 0 and is independent of the replay UI (Phases 1–4).

| Task | Description | Completed | Date |
|------|-------------|-----------|------|
| TASK-038 | Add the hub method `UndoLastStroke(boardName)` to `Hubs/WhiteboardHub.cs` (`Hub<IWhiteboardClient>`) — the **single owner** of undo's log-append and broadcast: resolve userId via the existing `GetUserId()` hub helper (server-assigned, the same helper used by `SendStroke`/`JoinBoard`/`MoveCursor`); normalize the route slug to its canonical `boardId` once; find the caller's last removable stroke via `StrokeEventService.GetLastRemovableStrokeByUserAsync(boardId, userId, cancellationToken)` (TASK-003 — a bounded index-backed query, not a full-history fold). If none, no-op. Otherwise append a `Remove` `StrokeEvent` for that stroke first (`AppendEventAsync(boardId, EventType.Remove, stroke, cancellationToken)` — the authoritative log entry), then broadcast `StrokeRemoved(strokeId)` to the group, consistent with the log-first ordering of `SendStroke` (TASK-005). Forward a `CancellationToken` (e.g. `Context.ConnectionAborted`) to both service calls. Per GUD-009, extend `IWhiteboardClient` with `StrokeRemoved(strokeId)` and broadcast via the typed proxy, never `SendAsync`. Note: member-access enforcement on private boards is layered onto this method by [feature-board-administration-1.md](./feature-board-administration-1.md), consistent with the guard on `SendStroke`. | ✅ | 2026-06-17 |
| TASK-039 | Add an undo control to the `wwwroot/index.html` toolbar (`<button id="btn-undo">↶ Undo</button>`) wired to call `UndoLastStroke(currentBoardName)`. | ✅ | 2026-06-17 |
| TASK-040 | In `wwwroot/js/connection.js` register a `StrokeRemoved(strokeId)` handler; in `wwwroot/js/app.js` handle it by removing the stroke from the local canvas state and re-rendering. | ✅ | 2026-06-17 |
| TASK-041 | Add integration test `Tests/UndoTests.cs` — `UndoLastStroke` removes only the caller's most recent stroke, appends a `Remove` event, and broadcasts `StrokeRemoved`; repeated undo removes strokes in reverse draw order; undo by a caller with no strokes is a no-op; one user's undo never removes another user's stroke. | ✅ | 2026-06-17 |

## 3. Alternatives

- **ALT-001**: Server-side video rendering (FFmpeg) to produce MP4 — rejected; adds heavy server dependency, eliminates interactivity (seek/pause), and increases infrastructure cost
- **ALT-002**: WebGL-based canvas rendering for replay — rejected; raw Canvas 2D API is sufficient for stroke replay and avoids WebGL complexity for a drawing app
- **ALT-003**: Stream all events (history + incremental) via SignalR during join instead of REST history fetch — rejected; REST with fixed oldest-first pagination enables proxy caching of immutable history; incremental updates via SignalR hub method keeps concerns separated and scales better for large boards
- **ALT-004**: Store pre-computed replay timeline in MongoDB — rejected; timeline computation is lightweight (client-side) and storing it would create stale data on every undo or hide operation
- **ALT-005**: Newest-first pagination — rejected; it shifts page boundaries on every new stroke (breaking immutable-page caching) and would force the join flow to read the last page for the oldest, not newest, timestamp. Oldest-first keeps pages `1..N-1` immutable and the final page the only mutable one
- **ALT-006**: Materialize/fold the entire event log in memory server-side (e.g. to derive current state or to find undo's target) — rejected; it does not scale with history size. The server uses bounded, index-backed queries instead (paginated reads, since-timestamp reads, a single-document last-removable-stroke lookup); folding the full log into a picture happens only on the client, which already streams the paginated history

## 4. Dependencies

- **DEP-001**: `Services/StrokeEventService.cs` (+ `IStrokeEventService`) — introduced by this plan (Phase 0); persistence/query layer: `AppendEventAsync`, `GetEventsPageAsync`, `GetEventsSinceAsync`, and the bounded `GetLastRemovableStrokeByUserAsync` for undo
- **DEP-002**: `Services/BoardService.cs` — the history route normalizes the URL slug to its canonical `boardId` (the board `_id`) at the boundary; `BoardService.GetBoardAsync(boardId)` confirms the board exists before querying the event log (no name→id lookup needed)
- **DEP-003**: `Middleware/UserIdentityMiddleware.cs` (MVP) — resolves userId from the HttpOnly cookie / connection context. Consumed only by the undo hub method (`UndoLastStroke`, Phase 5) to match the caller against `Stroke.UserId`; the REST history endpoint needs **no** caller identity (REQ-007)
- **DEP-004**: `Models/StrokeEvent.cs` — introduced by this plan (Phase 0); time-series document with `Type`, `Stroke` (containing `UserId`, `Id`, `Points[].TimeOffset`), and `Timestamp` (the `timeField`)
- **DEP-005**: `Models/Point.cs` — must include `TimeOffset` (`ushort`, milliseconds since stroke start; ≤ ~65 s, no sub-millisecond precision — serialized by the driver as BSON Int32, half the storage of the former `long`, which matters as every point is persisted in the event log)
- **DEP-006**: `Models/Stroke.cs` — must carry a stable stroke `Id` and the drawing `UserId`: undo targets and broadcasts a specific stroke (`StrokeRemoved(strokeId)`) and matches ownership by `UserId`, the event log uses `Id` as the tiebreaker when two events share a `Timestamp`, and `Id` is the natural idempotency / de-duplication key for events

## 5. Files

- **FILE-001**: `Program.cs` — Add `MapGet("/api/boards/{name}/history/{pageNumber}", ...)` endpoint registration (path-based pagination) with `TypedResults`, the explicit `Results<Ok<HistoryPageResponse>, BadRequest, NotFound>` return shape, and OpenAPI metadata (`.WithName`/`.WithSummary`/`.Produces`) following the MVP snapshot endpoint's metadata pattern; register `IStrokeEventService` in DI and call its `EnsureIndexesAsync` at startup; **remove** the `GET /api/boards/{name}/snapshot` endpoint and the `MapBoardSnapshot` helper (TASK-007)
- **FILE-002**: `wwwroot/js/replay.js` — ReplayEngine class: history loading, timeline computation, animation loop, gap compression, seek, speed control
- **FILE-003**: `wwwroot/index.html` — Add replay button + replay controls (under-canvas scrubber, play/pause/stop, speed selector, timestamp display) and the undo button (Phase 5)
- **FILE-004**: `wwwroot/css/style.css` — Add replay control styles (scrubber div under the canvas, replay-mode controls hidden by default)
- **FILE-005**: `Tests/ReplayHistoryEndpointTests.cs` — Integration tests for the history REST endpoint
- **FILE-006**: `Models/StrokeEvent.cs` — Append-only event log document introduced by this plan (Phase 0); `Type` (Add/Remove), embedded `Stroke`, `Timestamp` (the `timeField`); stored in a native time-series collection keyed by `BoardId` (the `metaField`)
- **FILE-007**: `Services/StrokeEventService.cs` (+ `IStrokeEventService`) — Event append and query operations including the bounded `GetLastRemovableStrokeByUserAsync` for undo, plus an idempotent `EnsureIndexesAsync` that creates the time-series collection and its secondary indexes (Phase 0); registered in DI in `Program.cs`
- **FILE-008**: `Services/MongoDbContext.cs` — Add the `StrokeEvents` collection accessor (extends the MVP context); the time-series collection itself is created programmatically via `EnsureIndexesAsync` (FILE-007), since a time-series collection cannot be created lazily by `GetCollection<>` (Phase 0)
- **FILE-009**: `Hubs/WhiteboardHub.cs` (+ `Hubs/IWhiteboardClient.cs`) — Layer `AppendEventAsync(..., EventType.Add, ...)` onto the core `SendStroke` and remove its `AddStrokeToSnapshotAsync` call (Phase 0); extend `JoinBoard` into the atomic `JoinBoard(boardName, sinceTimestamp)` that subscribes then replays the tail and drops the snapshot/`LoadSnapshot` path, removing `LoadSnapshot` from `IWhiteboardClient` (Phase 1); add the `UndoLastStroke` method and the `StrokeRemoved` client event (Phase 5)
- **FILE-010**: `Tests/StrokeEventServiceTests.cs` — Integration tests for the event log (relocated from the MVP; exercises a test database)
- **FILE-011**: `wwwroot/js/connection.js` — Register the `StrokeRemoved` handler (Phase 5)
- **FILE-012**: `wwwroot/js/app.js` — Join-time history population via the shared `fetchAllHistory` helper + atomic `JoinBoard(boardName, sinceTimestamp)` (Phase 1); remove the undone stroke from the local canvas and re-render (Phase 5)
- **FILE-013**: `Tests/UndoTests.cs` — Undo integration tests (Phase 5)
- **FILE-014**: `Dtos/HistoryPageResponse.cs` + `Dtos/StrokeEventResponse.cs` — Response DTOs for the history endpoint (GUD-003), each with a `<summary>` XML doc comment; reuse the existing `StrokeResponse`/`PointResponse` DTOs (Phase 1, TASK-010)
- **FILE-015**: `canvas.http` — Add sample requests for the history endpoint (page 1, a non-existent page → `404`, and an `If-Modified-Since` conditional → `304`) and remove the retired `/snapshot` request (Phase 1, TASK-016)
- **FILE-016**: `Models/Board.cs` — Remove the `ActiveStrokes` snapshot field (TASK-007); board state is now derived from the event log
- **FILE-017**: `Services/BoardService.cs` (+ `IBoardService`) — Remove `GetSnapshotAsync` and `AddStrokeToSnapshotAsync` and the `ActiveStrokes` `SetOnInsert` initialization (TASK-007)
- **FILE-018**: `Dtos/BoardSnapshotResponse.cs` — Deleted; the snapshot REST endpoint it served is removed (TASK-007)

## 6. Testing

- **TEST-001**: Integration test — `GET /api/boards/{name}/history/{pageNumber}` returns paginated events oldest-first with correct `totalPages` and `totalEvents` counts
- **TEST-002**: Integration test — a complete page (`pageNumber < totalPages`) carries `Cache-Control: public, max-age=31536000` (no `immutable`) and a `Last-Modified` equal to its most recent stroke; the final page carries `Cache-Control: no-cache` with `Last-Modified` equal to its most recent stroke; requesting a non-existent page returns `404`; all pages combined contain the complete unfiltered history
- **TEST-003**: Integration test — a conditional GET with `If-Modified-Since` returns `304 Not Modified` for both an unchanged **final** page and a **complete** page (manual-reload revalidation)
- **TEST-004**: Unit test — `computeTimeline()` with events [t=0s, t=1s, t=15s, t=16s] compresses the 14s gap to 3s, resulting in totalDuration = 0 + 1 + 3 + 1 = 5s of playback
- **TEST-005**: Unit test — `seek(0.5)` on a timeline with 4 strokes renders exactly the first 2 strokes completely
- **TEST-006**: Integration test — `StrokeEventService`: `AppendEventAsync` stamps a server `Timestamp`, `GetEventsPageAsync` returns events oldest-first with correct counts, a re-sent `Add` with a duplicate stroke `Id` is not double-logged, and `GetLastRemovableStrokeByUserAsync` returns the caller's most recent non-removed `Add` (Phase 0 event log)
- **TEST-007**: Integration test — `UndoLastStroke` removes only the caller's most recent stroke, appends a `Remove` event, and broadcasts `StrokeRemoved(strokeId)`; repeated undo removes strokes in reverse draw order
- **TEST-008**: Integration test — Undo by a caller with no strokes is a no-op, and a caller's undo never removes another user's stroke
- **TEST-009**: Manual test — Full replay with gap compression behaves as described in Phase 4 (TASK-035–037)

## 7. Risks & Assumptions

- **RISK-001**: Very large boards (10,000+ events) may cause slow initial fetch — mitigated by pagination and potential future streaming/chunked loading (the keyframe + lazy-history approach is specified in [feature-history-compaction-1.md](./feature-history-compaction-1.md))
- **RISK-002**: `requestAnimationFrame` timing inconsistencies across browsers may cause slight playback drift — mitigated by using `performance.now()` for elapsed time rather than frame counting
- **RISK-003**: Seek to arbitrary position requires re-rendering all prior strokes (O(n) for n events before seek point) — acceptable for demo scale; could be optimized with periodic snapshots for very large boards (the keyframe/segment approach is specified in [feature-history-compaction-1.md](./feature-history-compaction-1.md))
- **RISK-004**: Path-based pagination resolves a page with `skip((pageNumber-1) * pageSize).limit(pageSize)`, which is O(n) in the skipped count — deep pages on very large boards get progressively more expensive to serve. Acceptable at demo scale (and complete pages are served from cache, so deep pages are fetched at most once), and mitigated long-term by the keyframe compaction in [feature-history-compaction-1.md](./feature-history-compaction-1.md), which bounds how much raw history is paged
- **ASSUMPTION-001**: The `Stroke`/`Point` temporal fields (`Timestamp`, `Points[].TimeOffset`) are populated by the MVP whiteboard implementation; this plan's Phase 0 records them into `StrokeEvent` documents
- **ASSUMPTION-002**: Canvas 2D context can be cleared and re-rendered fast enough for seek operations (target: <100ms for 1000 strokes)
- **ASSUMPTION-003**: Replay is read-only — drawing tools are disabled during playback and re-enabled on stop
- **ASSUMPTION-004**: Native time-series collections cannot enforce unique indexes, so event insertion is at-least-once: exactly-once is approximated by insert-time dedup in `StrokeEventService` and read-side dedup by the stable stroke `Id` on the client (and on the `GetEventsSinceAsync` boundary). For throwaway demo data this is sufficient; no DB-level uniqueness constraint is required
- **ASSUMPTION-005**: Clients build board state from the event stream (no snapshots). Stop replay resyncs by refetching all events from the history endpoint. Later, keyframes ([feature-history-compaction-1.md](./feature-history-compaction-1.md)) will optimize large-board resync

## 8. Related Specifications / Further Reading

- [Main whiteboard implementation plan](./feature-collaborative-whiteboard-1.md) — parent plan containing data model, SignalR hub, and persistence layer
- [History access & moderation feature plan](./feature-history-access-moderation-1.md) — layers the membership access gate and owner-controlled HiddenRanges filtering onto the history endpoint defined here
- [Visibility moderation feature plan](./feature-visibility-moderation-1.md) — the live-snapshot counterpart of cut-off moderation; defines the HiddenRange model reused for history filtering
- [History compaction feature plan](./feature-history-compaction-1.md) — keeps large-history boards fast to load via derived keyframes over the event log this plan introduces, while retaining full history for on-demand replay
- [requestAnimationFrame documentation](https://developer.mozilla.org/en-US/docs/Web/API/window/requestAnimationFrame)
- [Canvas 2D API — Path2D and drawing](https://developer.mozilla.org/en-US/docs/Web/API/CanvasRenderingContext2D)
- [performance.now() for high-resolution timing](https://developer.mozilla.org/en-US/docs/Web/API/Performance/now)
